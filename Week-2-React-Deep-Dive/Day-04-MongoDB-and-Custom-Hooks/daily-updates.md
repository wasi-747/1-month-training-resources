# Daily Progress Updates — Week 2, Day 4 (MongoDB Practice)

## Daily TODO
- Practice MongoDB CRUD operations (insertOne, insertMany, find, updateOne, deleteOne).
- Learn query filtering using comparison and logical operators ($gte, $lte, $in, $and, $or).
- Practice array updates ($push, $addToSet, $pull) and field updates ($set, $inc).
- Learn basic data grouping using Aggregation Pipeline ($match, $group, $sort).
- Push practice files and daily logs to GitHub.

---

## Mid-Day Progress Update

Completed:
- Practiced MongoDB CRUD operations (insertOne, insertMany, updateOne, deleteOne).

In Progress:
- MongoDB queries (Comparison operators, Logical operators, and Aggregation pipelines).

Blockers: None.

---

## End of Day (EOD) Update

Completed Today:
- Practiced core MongoDB CRUD operations (insertOne, insertMany, updateOne, deleteMany).
- Learned query filtering using comparison operators ($gt, $gte, $lt, $lte, $in, $ne) and logical operators ($and, $or).
- Practiced array updates ($push, $addToSet, $pull) and field updates ($set, $inc).
- Learned basic data grouping and calculations using Aggregation Pipeline ($match, $group, $sort).
- Explored basic indexing and pushed all practice code and notes to GitHub.

GitHub Repository: https://github.com/wasi-747/1-month-training-resources
Blockers: None
Tomorrow's Plan: Learn how to connect MongoDB with Next.js API routes.
